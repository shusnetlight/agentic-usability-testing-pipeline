# Usability Testing Pipeline

This project analyzes moderated usability test data (think-aloud protocol, tested against an existing website/app) through a multi-agent pipeline to produce actionable UX insights. Each agent is invoked by name, writes output to files, and waits for your review before the next stage runs.

---

## Session Start & Status Checks

**Every time this folder is opened in Claude Code, do the following before responding to any user message.** This same check also runs on demand whenever the researcher asks for a status update (e.g. "what's the status?", "what's next?") — it is a built-in behavior of this file, not a separate agent to invoke.

1. Check the project state by reading these locations (existence and modification time only — do not read file contents):
   - `input/ui-mockup/` — any `.html` files present?
   - `input/ui_context.md` — does it exist? Does it contain "Extracted from" (Extractor-generated) or not (manually written)?
   - `input/interview_guide.md` — exists?
   - `input/interviews/` — how many participant folders?
   - `output/coded/` — how many files?
   - `output/reconciled/` — how many files?
   - `output/code_mapping.md` — exists?
   - `output/themes.md` — exists? Contains `⚠️ Advocatus` blocks (not yet reviewed)?
   - `output/insights.md` — exists? Contains `⚠️ Advocatus` blocks?
   - `output/report.md` — exists?

2. Report a compact status summary and the single recommended next step. Format:

```
**[Project Name] — Pipeline Status**

✅ UI extracted (Extractor)        or  ⚠️ ui_context.md exists but was not generated by Extractor
✅ Interview guide present
✅ 5 participants found
✅ 5 interviews coded
✅ Codes reconciled
⬜ Themes not yet synthesized
⬜ ...

→ **Next step**: "Synthesizer, find themes"
```

   Use ✅ for complete, ⬜ for not yet done, ⚠️ for needs attention (e.g. Advocatus blocks unreviewed, severity discrepancies flagged).

3. If the project appears brand new (no output files, no ui_context.md), show the Quick Start steps instead.

4. Keep the status report to under 15 lines. Do not read file contents for the status check — only check existence and look for the specific markers noted above.

5. If asked "what's next?" only (without a full status request), skip the checklist and answer with just the next-step line:

```
→ **Next step**: "[exact invocation command]"
[One sentence explaining why]
```

---

## Quick Start

1. Add your website export (HTML) to `input/ui-mockup/` — this is the "User Interface Mockup"
2. Say **"Extractor, analyze UI"** → generates `input/ui_context.md` from the actual website
3. Put your interview guide in `input/interview_guide.md`
4. Edit `config.md` with your project details — including **"Analysis output"** under Language to set the output language (e.g. German, English, French)
5. Place interview transcripts/notes in `input/interviews/participant_XX/` (supported formats: `.md`, `.txt`, `.vtt`)
6. Run the pipeline agents in order (see below)

---

## The Seven Agents

Pipeline status and "what's next" is not a separate agent — it's handled automatically by this file (see Session Start & Status Checks above). Just ask.

| Agent | What it does | Output |
|---|---|---|
| **Extractor** | Reads website HTML from `input/ui-mockup/` and generates the UI reference document | `input/ui_context.md` |
| **Coder** | Codes a single interview (inductive, open coding) | `output/coded/participant_XX.md` |
| **Reconciler** | Normalizes code labels across all interviews | `output/code_mapping.md` + `output/reconciled/` |
| **Synthesizer** | Clusters codes into themes | `output/themes.md` |
| **Advocatus** | Devil's advocate — challenges the UI context, themes, and insights with inline annotations | rewrites `input/ui_context.md`, `output/themes.md`, or `output/insights.md` in place |
| **Interpreter** | Generates prioritized insights + recommendations | `output/insights.md` |
| **Reporter** | Assembles the final stakeholder-ready report | `output/report.md` |

---

## How to Run

### Stage 0: Extract UI Context

**Say:** "Extractor, analyze UI"

**What happens:** A single agent using the prompt from `agents/extractor.md`:
1. Reads `config.md` for the project name and output language
2. Scans `input/ui-mockup/` for HTML files and reads the website export
3. Parses the navigation structure, page names, key components, and interactive elements
4. Writes `input/ui_context.md` — a shared vocabulary and component map for all downstream agents

**Re-run when**: The UI has changed since the last extraction, or you add a new website export.

**Review before proceeding.** Check that:
- All navigation tabs and pages are correctly identified
- Component names match what you see in the actual interface
- The Naming Conventions table covers everything a participant is likely to touch

You can manually edit `input/ui_context.md` after extraction to add context the HTML alone can't reveal (e.g. known bugs, intentionally disabled features in staging).

---

### Stage 1: Coding

**Say:** "Coder, code all interviews"

**What happens:** For each participant folder in `input/interviews/`, spawn a sub-agent using the prompt from `agents/coder.md`. Each sub-agent:
1. Reads `config.md`, `input/interview_guide.md`, and `input/ui_context.md` for context
2. Reads that participant's transcript and/or notes from `input/interviews/participant_XX/`
3. Applies open coding (no predefined codebook — codes emerge from the data)
4. Writes the coded output to `output/coded/participant_XX.md`

All participants are coded **in parallel** (one sub-agent per participant).

**Supported transcript formats**: `.md`, `.txt`, `.vtt` (WebVTT — exported from Zoom, Teams, Otter.ai, etc.). The Coder handles all three. VTT files produce an additional Timestamp column in the coded output so observations can be traced back to the exact moment in the recording.

**To re-code a single participant:** "Coder, re-code participant 05" (optionally add guidance, e.g., "focus on the calendar interaction")

**Review before proceeding.** Check that:
- Quotes are verbatim (not paraphrased)
- Code labels are descriptive and grounded in what the participant said/did
- Severity ratings (0-4) match what you observed
- No observations were missed

---

### Stage 1b: Challenge UI Context (Devil's Advocate)

**Say:** "Advocatus, challenge UI context"

**What happens:** A single agent using Mode C from `agents/advocatus.md`:
1. Reads `input/ui_context.md` and every file in `output/coded/`
2. Cross-checks every **UI Element** reference in the coded files against `ui_context.md`
3. Appends a single `⚠️ Advocatus — UI Context Review` section to the end of `input/ui_context.md`, flagging missing elements, naming mismatches, undocumented states, and (informationally) orphaned context entries
4. Does NOT alter any existing content in `ui_context.md` — only appends the review section

**Why this runs here**: The Coder is the only agent besides Interpreter/Reporter that actively reads `ui_context.md` — it copies UI Element names from it into every coded observation. Reconciler and Synthesizer never read `ui_context.md` again; they just pass that UI Element field through unchanged. So any gap or wrong name in `ui_context.md` gets frozen into all coded files right after Stage 1, and nothing downstream re-checks it until Interpreter/Reporter read it again at the very end — too late to fix the coded data itself. Running the check here, right after coding, is the earliest point where real participant data exists to check `ui_context.md` against, and the last point where a fix is still cheap.

**Review the findings.** For each flagged item:
- Missing element / naming mismatch → edit `input/ui_context.md` directly, then delete that line from the review section
- Disagree → delete the line without changing `ui_context.md`
- Orphaned context items are informational only — act on them or ignore them
- When done, no `⚠️ Advocatus — UI Context Review` section should remain in the file

If a naming mismatch actually needs a fix in a coded file (not in `ui_context.md`), the finding will say so — go fix that file directly.

---

### Stage 2: Reconciliation

**Say:** "Reconciler, normalize codes"

**What happens:** A single agent using the prompt from `agents/reconciler.md`:
1. Reads all files in `output/coded/`
2. Identifies code labels that describe the same phenomenon across participants
3. Creates a mapping table and writes it to `output/code_mapping.md`
4. Produces reconciled coded files in `output/reconciled/participant_XX.md` with canonical labels
5. Flags severity discrepancies (does NOT auto-resolve them)

**Review before proceeding.** Check `output/code_mapping.md`:
- Are the label groupings correct? (split or merge if needed)
- Resolve any flagged severity discrepancies
- After your edits, say "Reconciler, re-apply mapping" if you changed the mapping

---

### Stage 3: Theme Synthesis

**Say:** "Synthesizer, find themes"

**What happens:** A single agent using the prompt from `agents/synthesizer.md`:
1. Reads all reconciled files from `output/reconciled/`
2. Reads `input/interview_guide.md` for task context
3. Clusters codes into themes via affinity mapping
4. Writes `output/themes.md` with theme cards including frequency, severity, evidence

**Review before proceeding.** Check that:
- Theme groupings make sense (merge or split as needed)
- Frequency counts are correct
- All themes have evidence from at least 2 participants
- Single-participant findings are in the "Notable individual findings" section

---

### Stage 3b: Challenge Themes (Devil's Advocate)

**Say:** "Advocatus, challenge themes"

**What happens:** A single agent using the prompt from `agents/advocatus.md`:
1. Reads `output/themes.md` and all reconciled coded files
2. For each theme, raises up to five challenges: evidence strength, clustering validity, alternative explanations, severity inflation, contradictory evidence
3. Inserts a `⚠️ Advocatus` annotation block directly after each theme card in `output/themes.md`
4. Does NOT alter any existing content — only adds annotations

**Review the annotations.** For each `⚠️ Advocatus` block:
- If the challenge is valid: edit the theme above it (adjust severity, qualify the theme name, split it, or add a note)
- If you disagree: delete the annotation block
- When done, no `⚠️ Advocatus` blocks should remain in the file — they are working notes, not part of the final analysis

---

### Stage 4: Insight Generation

**Say:** "Interpreter, generate insights"

**What happens:** A single agent using the prompt from `agents/interpreter.md`:
1. Reads `output/themes.md`
2. Reads `input/interview_guide.md` and `input/ui_context.md`
3. Transforms each theme into a structured insight (observed/because/which means)
4. Prioritizes using the severity x frequency matrix
5. Writes `output/insights.md`

**Review before proceeding.** Check that:
- The "because" clause (mental model inference) is reasonable
- Priority ratings match the matrix
- Recommendations are specific and actionable
- Evidence chains trace back to real citations

---

### Stage 4b: Challenge Insights (Devil's Advocate)

**Say:** "Advocatus, challenge insights"

**What happens:** A single agent using the prompt from `agents/advocatus.md`:
1. Reads `output/insights.md` and `output/themes.md`
2. For each insight, raises up to five challenges: "because" clause evidence, recommendation–cause alignment, priority justification, unintended consequences, alternative recommendations
3. Inserts a `⚠️ Advocatus` annotation block directly after each insight card in `output/insights.md`
4. Does NOT alter any existing content — only adds annotations

**Review the annotations.** Same process as Stage 3b:
- Address valid challenges by editing the insight above the annotation
- Delete annotations you disagree with
- Clear all `⚠️ Advocatus` blocks before proceeding to the report

---

### Stage 5: Report Assembly

**Say:** "Reporter, write the report"

**What happens:** A single agent using the prompt from `agents/reporter.md`:
1. Reads all previous outputs + `templates/report_template.md`
2. Compiles the final report
3. Writes `output/report.md`

**Review the final report.** You can ask for adjustments:
- "Reporter, make the executive summary shorter"
- "Reporter, reorder recommendations by UI area"

---

## How to Iterate

| You changed... | Re-run from... |
|---|---|
| Website HTML in input/ui-mockup/ | Extractor → then Coder onward if component names changed |
| One raw coded file | Reconciler onward |
| Added a new participant | Coder (just that one) → Advocatus UI context check → Reconciler onward |
| Edited ui_context.md after Advocatus review | Reconciler onward (only if canonical names changed) |
| Edited code_mapping.md | Reconciler (re-apply) + Synthesizer onward |
| Edited a reconciled file | Synthesizer onward |
| Edited themes.md after Advocatus review | Interpreter onward |
| Edited insights.md after Advocatus review | Reporter |

---

## Reusing for a New Project

1. Duplicate this entire folder
2. Edit `config.md` with your new project details
3. Replace the HTML in `input/ui-mockup/` with the new project's website export
4. Say "Extractor, analyze UI" to regenerate `input/ui_context.md`
5. Replace `input/interview_guide.md` with the new interview protocol
6. Add participant folders to `input/interviews/`
7. Clear `output/` (delete all generated files)
8. Open in Claude Code and run the pipeline

The agent prompts in `agents/` and the report template are project-agnostic.

---

## File Reference

- `config.md` — Project-specific context (edit per project)
- `agents/extractor.md` — Extractor agent prompt
  (pipeline status/next-step guidance lives in this file's Session Start section, not a separate agent)
- `agents/coder.md` — Coder agent prompt
- `agents/reconciler.md` — Reconciler agent prompt
- `agents/synthesizer.md` — Synthesizer agent prompt
- `agents/advocatus.md` — Advocatus agent prompt (Mode A: challenge themes, Mode B: challenge insights, Mode C: challenge UI context)
- `agents/interpreter.md` — Interpreter agent prompt
- `agents/reporter.md` — Reporter agent prompt
- `input/ui-mockup/` — Website HTML export ("User Interface Mockup") — source for Extractor
- `input/ui_context.md` — Auto-generated UI reference document; also the target of Advocatus Mode C's review section (can be manually edited)
- `input/interview_guide.md` — The interview protocol
- `input/interviews/participant_XX/` — Raw interview data (transcript.md/vtt, notes.md)
- `output/coded/` — Raw coded files from Coder
- `output/reconciled/` — Normalized coded files from Reconciler
- `output/code_mapping.md` — Code label mapping table from Reconciler
- `output/themes.md` — Theme cards from Synthesizer
- `output/insights.md` — Insight cards from Interpreter
- `output/report.md` — Final report from Reporter
- `templates/report_template.md` — Report structure template
